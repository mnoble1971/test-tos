# From BMH, install create-centos.bash
# bash ./create-centos.bash {vm-name}
# run bash ./docker_install.sh  on the new vm to install all docker
#   -- test docker with:  
#        docker run hello-world